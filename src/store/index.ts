
import Vue from 'vue'
import Vuex from 'vuex'

const state = {
    count: 100
}
// 
const mutations = {
 
}
// 异步
const actions = {
   
}
// 获取
const getters = {
   
}

export default new Vuex.Store({
    state,
    mutations,
    actions,
    getters
})